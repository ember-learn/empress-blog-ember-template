
{{#let (get model.errors 0) as |error|}}

<header class="page-head error-head">
    <h1 class="page-head-title">{{error.status}}</h1>
    <p class="page-head-description">{{error.title}}</p>
    <LinkTo @route="index">Go to the front page →</LinkTo>
</header>

{{/let}}
